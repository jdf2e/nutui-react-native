import {
  StyleSheet
} from 'react-native';

export default () => {
  return StyleSheet.create({
    container: {
      position: 'relative',
      flexDirection: 'row',
      flexGrow: 0,
      flexShrink: 0,
      flexBasis: 'auto'
    }
  });
};
